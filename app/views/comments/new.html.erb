<h2>New Comment</h2>
<% form_for [@commentable, Comment.new] do |form| %>
<!-- user name, enter before  -->
  <ol class="formList">
    <li>
      <%= form.label :body %>
      <%= form.text_area :body, :rows => 5 %>
    </li>
    <li><%= submit_tag "Add comment" %></li>
  </ol>
<% end %>